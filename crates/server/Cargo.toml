[package]
name = "server"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "vaylix"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
anyhow = "1"
clap = { version = "4.6.1", features = ["derive"] }
