[package]
name = "server"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "vaylix"
path = "src/main.rs"

[dependencies]
command = { path = "../command" }
engine = { path = "../engine" }
transport = { path = "../transport" }
clap = { version = "4.6.1", features = ["derive"] }
thiserror = "2.0.18"
