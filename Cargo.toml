[workspace]
resolver = "2"

members = [
    "crates/command",
    "crates/engine",
    "crates/server",
    "crates/client",
    "crates/transport",
]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
strip = true
